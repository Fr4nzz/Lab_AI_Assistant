# =============================================================================
# BACKEND CONFIGURATION (Python)
# =============================================================================

# Gemini API Keys (comma-separated for rotation)
GEMINI_API_KEYS=your_api_key_1,your_api_key_2,your_api_key_3

# Gemini Model (gemini-flash-latest = Gemini 2.5 Flash, gemini-3-flash-preview = Gemini 3 Flash)
GEMINI_MODEL=gemini-flash-latest

# Target URL for the laboratory system
TARGET_URL=https://laboratoriofranz.orion-labs.com/

# Browser to use: msedge, chrome, or chromium
# msedge = Uses your installed Microsoft Edge (no download needed)
# chrome = Uses your installed Google Chrome (no download needed)
# chromium = Uses Playwright's Chromium (requires: playwright install chromium)
BROWSER_CHANNEL=msedge

# Browser data directory (for session persistence)
BROWSER_DATA_DIR=./browser_data

# Debug mode
DEBUG=false

# =============================================================================
# FRONTEND CONFIGURATION (Nuxt)
# =============================================================================

# Backend URL - where the Python backend is running
BACKEND_URL=http://localhost:8000

# Database path for SQLite (chat history, users)
DATABASE_PATH=./data/lab-assistant.db

# Session password - 32+ character secret for encrypting session cookies
# Generate with: openssl rand -base64 32
# REQUIRED for production, optional for dev (will use default)
NUXT_SESSION_PASSWORD=

# Google OAuth credentials (optional - skip for local dev without auth)
# Get from: https://console.cloud.google.com/apis/credentials
NUXT_OAUTH_GOOGLE_CLIENT_ID=
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=

# Comma-separated list of allowed emails (optional - leave empty to allow all)
ALLOWED_EMAILS=

# Comma-separated list of admin emails (can manage users and update app)
ADMIN_EMAILS=

# OpenRouter API key (optional - for fallback or alternative models)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# OpenRouter model to use (optional - has sensible default)
# Default: google/gemini-2.5-flash-preview-05-20
OPENROUTER_MODEL=

# PostgreSQL Database URL (optional - for production deployment)
# If set, overrides DATABASE_PATH. Format: postgresql://user:password@host:port/database
# Leave empty to use SQLite (recommended for local development)
DATABASE_URL=

# =============================================================================
# TELEGRAM BOT CONFIGURATION (Optional)
# =============================================================================

# Telegram Bot Token (get from @BotFather)
# See docs/TELEGRAM_BOT_SETUP.md for instructions
TELEGRAM_BOT_TOKEN=

# Restrict bot to specific Telegram user IDs (comma-separated, optional)
# Leave empty to allow all users
# Find your user ID by messaging @userinfobot on Telegram
TELEGRAM_ALLOWED_USERS=

# Frontend URL (where the Nuxt frontend is running)
# The Telegram bot connects to the Frontend API for chat management
FRONTEND_URL=http://localhost:3000

# Internal API key for Telegram bot authentication (optional for local dev)
# If set, the Telegram bot will use this key to authenticate with the Frontend API
# Generate with: openssl rand -hex 32
INTERNAL_API_KEY=

# Cloudflare Tunnel URL (optional - for generating web links in Telegram)
# Auto-detected if using cloudflare-quick-tunnel.bat, or set manually
CLOUDFLARE_TUNNEL_URL=

# =============================================================================
# CLOUDFLARE TUNNEL CONFIGURATION (Optional)
# =============================================================================

# WhatsApp phone for tunnel URL notifications (with country code)
# Used by cloudflare-quick-tunnel-notify.bat
WHATSAPP_NOTIFY_PHONE=
