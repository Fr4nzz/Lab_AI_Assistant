# =============================================================================
# NOTE: Use the .env file in the PROJECT ROOT instead of this one.
# =============================================================================
#
# This directory's .env is deprecated. All configuration should be in:
#   /.env
#
# Copy /.env.example to /.env and configure there.
# The start-dev.bat script loads the root .env and passes all variables.
#
